#include <io.fh>
#include <memory.fh>
#include <cbridge.fh>

func main(): Int {
    let wstr: &WideChar = alloc(sizeof(WideChar), 3u);
    wstr[0] = W'Δ';
    wstr[1] = 10c;
    wstr[2] = 0c;
    wprint(wstr);
    dealloc(wstr);
    return 0;
}
